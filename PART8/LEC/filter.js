//filters the elements of an array according to the condition and store it in an another array
let arr = [1,2,3,4,5,6,7,8,9];

let evenNums = arr.filter((el) => (el % 2 == 0));

console.log(evenNums);

